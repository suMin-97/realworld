import Container from "@mui/material/Container";
import { Typography } from "@mui/material";

const HomePage = () => {
  return (
    <Container fixed>
      <Typography variant="h1">Welcome Home Page</Typography>
    </Container>
  );
};

export default HomePage;
